<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.CazadorPalabras2" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Color primario: café medio -->
        <item name="android:colorPrimary">@color/brown_500</item>
        <!-- Color primario Variant -->
        <item name="android:colorPrimaryDark">@color/brown_700</item>
        <!-- Color de acento / secondary -->
        <item name="android:colorSecondary">@color/brown_300</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/brown_900</item>
        <!-- Texto sobre fondo café: blanco -->
        <item name="android:textColor">@color/white</item>
    </style>
</resources>